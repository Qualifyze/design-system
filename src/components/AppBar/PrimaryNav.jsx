import PropTypes from 'prop-types'

export default function AppBarPrimaryNav({ children }) {
  return children
}

AppBarPrimaryNav.dsName = 'AppBarPrimaryNav'
AppBarPrimaryNav.propTypes = { children: PropTypes.node }
AppBarPrimaryNav.defaultProps = { children: null }
