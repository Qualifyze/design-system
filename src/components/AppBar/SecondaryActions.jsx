import PropTypes from 'prop-types'

export default function AppBarSecondaryActions({ children }) {
  return children
}

AppBarSecondaryActions.propTypes = { children: PropTypes.node }
AppBarSecondaryActions.defaultProps = { children: null }
