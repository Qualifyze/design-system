name: Compressed Size

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Build and compare compressed file sizes
        uses: preactjs/compressed-size-action@271282ca25e92249d56092e277f42b5db527ccb0
